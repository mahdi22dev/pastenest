{
  "name": "pastenest",
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@8.4.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main",
    "prisma": "cd ./apps/api && npx prisma generate && cd ../.. && npm run build",
    "client": "cd ./apps/client && pnpm add ",
    "api": "cd ./apps/api && pnpm add "
  },
  "devDependencies": {
    "turbo": "^2.0.14"
  }
}
